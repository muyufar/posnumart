# 2026-09-06

- Live `beli-langsung` (member retail) muncul DataTables Ajax error (tn/7) saat ketik pencarian produk. Endpoint lama pakai SSP: `LIKE` di semua kolom (termasuk harga/stok/id), `SQL_CALC_FOUND_ROWS`, plus `COUNT(*)` seluruh tabel `barang` (~75rb baris).
- Diganti query ringan: cari hanya kode + nama, COUNT per cabang, request POST. Lib: `modules/penjualan/lib/beli-langsung-search-data-lib.php`. Footer tidak lagi re-init `#example1` jika sudah server-side.

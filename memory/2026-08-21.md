# 2026-08-21

- Fitur generate katalog promo di `produk-analisa`: filter kategori Nugrosir (`kategori_cabang = 0`), centang produk, output gambar brosur (html2canvas) atau teks list. Harga yang ditampil = harga retail. Gambar ikut jika `barang_gambar` terisi.
- Bug gambar katalog kosong (contoh Amago): katalog baca `barang_gambar` di baris cabang toko, sementara foto biasanya hanya di master Nugrosir (cabang 0). Fallback sekarang ambil gambar dari cabang 0 by `barang_kode`. Atribut `crossorigin` di tag img brosur dihapus karena bisa membuat foto tidak tampil.
- Header brosur: judul default `Katalog Produk` (bisa diubah manual), logo kiri pakai `dist/img/numart-logo.png`. Area foto produk berlatar putih.
- Fitur perbaikan HPP toko↔gudang: toko kirim permintaan (`hpp-perbaikan-toko` + tombol ✈️ di laporan kategori); gudang proses di `hpp-perbaikan-gudang` (update status + koreksi `keranjang_harga_beli` histori + sync master). Lib: `aksi/hpp-perbaikan-lib.php`.

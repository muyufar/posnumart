# Jangan izinkan unduhan langsung file rahasia (token Fonnte).
<Files "no.js">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order allow,deny
        Deny from all
    </IfModule>
</Files>

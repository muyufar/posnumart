# Folder export/ (legacy)

Skrip import/export Excel lama yang masih dipakai beberapa template bulk.

**Catatan reorganisasi:** File export utama aplikasi sudah dipindah ke `modules/{modul}/actions/`. Folder ini dipertahankan sementara untuk kompatibilitas template download (`download_template_*.php`) dan export lama yang masih direferensikan.

Vendor PhpSpreadsheet lokal: `export/vendor/` (terpisah dari root `vendor/`).
